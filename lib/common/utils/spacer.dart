import 'package:flutter/material.dart';

Widget hSpacer(double? width) {
  return SizedBox(
    width: width ?? 5,
  );
}

Widget vSpacer(double? height) {
  return SizedBox(
    height: height ?? 5,
  );
}
